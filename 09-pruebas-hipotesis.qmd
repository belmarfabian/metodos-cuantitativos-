# Pruebas de hipótesis {#hipotesis}

## Objetivos del capítulo {.unnumbered}

Al finalizar este capítulo, serás capaz de:

- Comprender la lógica de las pruebas de hipótesis estadísticas
- Formular hipótesis nula y alternativa de manera apropiada
- Interpretar valores p y niveles de significancia
- Distinguir entre significancia estadística y significancia práctica
- Reconocer y evitar errores tipo I y tipo II
- Realizar e interpretar pruebas de hipótesis en R
- Entender las limitaciones de las pruebas de hipótesis

## ¿Para qué sirven las pruebas de hipótesis?

Imagina que realizas una encuesta antes y después de una campaña publicitaria. Antes, 42% de las personas apoyaban al Candidato A. Después, 47% lo apoyan. La pregunta natural es: **¿la campaña fue efectiva, o esta diferencia de 5 puntos podría deberse simplemente a que encuestaste a personas diferentes?**

Este es el problema central que resuelven las pruebas de hipótesis: nos ayudan a distinguir entre **patrones reales** y **variación aleatoria**.

### La idea intuitiva

Las pruebas de hipótesis funcionan con una lógica de "abogado del diablo":

1. **Asumimos que NO hay efecto** (esta es la "hipótesis nula")
2. **Preguntamos:** Si realmente no hubiera efecto, ¿qué tan probable sería ver los datos que observamos?
3. **Si esa probabilidad es muy baja:** Entonces nuestra suposición inicial probablemente es falsa - sí hay un efecto real
4. **Si la probabilidad no es tan baja:** No tenemos suficiente evidencia para afirmar que hay un efecto

Es similar a un juicio: asumimos inocencia (no hay efecto), y solo cambiamos de opinión si la evidencia es muy fuerte.

::: {.callout-tip}
## Analogía del juicio

| Concepto estadístico | Equivalente judicial |
|---------------------|---------------------|
| Hipótesis nula | "El acusado es inocente" |
| Hipótesis alternativa | "El acusado es culpable" |
| Rechazar la hipótesis nula | Declarar culpable |
| No rechazar la hipótesis nula | Absolver (no declarar inocente, sino "no hay suficiente evidencia") |
| Valor p bajo | Evidencia fuerte en contra |

El sistema prefiere dejar libre a un culpable antes que encarcelar a un inocente. De manera similar, las pruebas estadísticas son conservadoras: preferimos no afirmar un efecto antes que afirmar uno que no existe.
:::

## Formulando hipótesis

### Hipótesis nula vs. alternativa

En toda prueba de hipótesis definimos dos opciones:

**Hipótesis nula (H₀):** La afirmación de "no hay efecto" o "no hay diferencia". Es nuestra posición por defecto.

**Hipótesis alternativa (H₁):** Lo que sospechamos que es verdad. Es lo que queremos demostrar con evidencia.

### Ejemplos de formulación

| Pregunta de investigación | Hipótesis nula (H₀) | Hipótesis alternativa (H₁) |
|--------------------------|---------------------|---------------------------|
| ¿La política redujo el desempleo? | El desempleo sigue igual que antes | El desempleo disminuyó |
| ¿Hay diferencia de participación entre hombres y mujeres? | No hay diferencia | Sí hay diferencia |
| ¿El programa mejoró las notas? | Las notas siguen iguales | Las notas aumentaron |
| ¿Existe relación entre educación y voto? | Son independientes | Están asociados |

::: {.callout-note}
## ¿Cuándo usar hipótesis unilateral vs. bilateral?

- **Bilateral (dos colas):** Cuando solo te importa si hay diferencia, sin importar la dirección. "¿Difieren los salarios entre hombres y mujeres?" (pueden ser mayores o menores)

- **Unilateral (una cola):** Cuando tienes expectativa clara de la dirección. "¿El nuevo tratamiento *mejora* los resultados?" (solo te importa si mejora, no si empeora)

En la práctica, las pruebas bilaterales son más comunes y conservadoras.
:::

## Entendiendo el valor p

### ¿Qué significa el valor p?

El **valor p** responde a una pregunta específica:

> "Si realmente no hubiera ningún efecto, ¿qué tan probable sería observar datos tan extremos como los míos?"

**Interpretación correcta:**

- **p = 0.03:** Si no hubiera efecto, solo el 3% de las veces veríamos resultados tan extremos. Es poco probable que sea casualidad.

- **p = 0.45:** Si no hubiera efecto, el 45% de las veces veríamos resultados como estos. Es completamente compatible con "no hay efecto".

- **p = 0.001:** Resultados así serían extremadamente raros (1 en 1000) si no hubiera efecto.

::: {.callout-warning}
## Interpretaciones INCORRECTAS del valor p

El valor p **NO** es:

❌ La probabilidad de que la hipótesis nula sea verdadera
❌ La probabilidad de que tu resultado sea incorrecto
❌ Una medida de qué tan grande es el efecto
❌ La probabilidad de que el resultado se replique

El valor p solo te dice qué tan sorprendentes serían tus datos si no hubiera efecto.
:::

### La regla de decisión

Por convención, usamos un **nivel de significancia** (llamado α) como punto de corte:

| Si el valor p... | Decisión | En palabras |
|-----------------|----------|-------------|
| p < 0.05 | Rechazamos H₀ | "Resultado estadísticamente significativo" |
| p ≥ 0.05 | No rechazamos H₀ | "No hay evidencia suficiente" |

::: {.callout-warning}
## ¡Cuidado con la palabra "significativo"!

En estadística, "significativo" **NO** significa "importante". Solo significa que el resultado es improbable bajo la hipótesis nula.

Un efecto puede ser:
- Estadísticamente significativo pero irrelevante (diferencia minúscula con muestra enorme)
- Importante pero no significativo (diferencia grande pero muestra pequeña)
:::

### ¿Por qué 0.05?

El umbral de 0.05 (5%) fue propuesto por R.A. Fisher en los años 1920 como "conveniente". No tiene nada de mágico. En otras áreas usan umbrales diferentes:

- Física de partículas: 0.0000003 (5 sigmas) - porque afirmar nuevas partículas es muy serio
- Estudios exploratorios: 0.10 - para no perder pistas interesantes
- Investigación clínica: A veces 0.01 - porque los errores tienen consecuencias graves

## Tipos de errores

Cuando tomamos una decisión estadística, podemos equivocarnos de dos maneras:

### Error Tipo I: Falso positivo

**Rechazar H₀ cuando en realidad es verdadera.**

Es como declarar culpable a un inocente. En investigación, significa afirmar que hay un efecto cuando en realidad no existe.

La probabilidad de cometer este error es exactamente α (el nivel de significancia que elegimos).

### Error Tipo II: Falso negativo

**No rechazar H₀ cuando en realidad es falsa.**

Es como absolver a un culpable. En investigación, significa no detectar un efecto que realmente existe.

| | H₀ es verdadera (no hay efecto) | H₀ es falsa (sí hay efecto) |
|---|:---:|:---:|
| **Rechazamos H₀** | Error Tipo I 😟 | ¡Decisión correcta! ✓ |
| **No rechazamos H₀** | Decisión correcta ✓ | Error Tipo II 😟 |

```{r}
#| label: fig-errores
#| fig-cap: "Visualización de los dos tipos de error"
#| code-fold: true

library(ggplot2)

# Crear datos para dos distribuciones
x <- seq(-4, 8, length.out = 1000)
h0 <- dnorm(x, mean = 0, sd = 1)
h1 <- dnorm(x, mean = 3, sd = 1)

datos <- data.frame(
  x = rep(x, 2),
  densidad = c(h0, h1),
  hipotesis = rep(c("H0: No hay efecto", "H1: Hay efecto real"), each = 1000)
)

valor_critico <- qnorm(0.95, mean = 0, sd = 1)

ggplot(datos, aes(x = x, y = densidad, color = hipotesis, fill = hipotesis)) +
  geom_line(linewidth = 1) +
  geom_area(data = subset(datos, hipotesis == "H0: No hay efecto" & x > valor_critico),
            alpha = 0.4) +
  geom_area(data = subset(datos, hipotesis == "H1: Hay efecto real" & x < valor_critico),
            alpha = 0.4) +
  geom_vline(xintercept = valor_critico, linetype = "dashed", color = "black") +
  annotate("text", x = valor_critico + 0.3, y = 0.35,
           label = "Valor crítico", hjust = 0, size = 3) +
  annotate("text", x = 2.5, y = 0.05,
           label = "Error Tipo I\n(α)", size = 3, color = "#E41A1C") +
  annotate("text", x = 0.5, y = 0.05,
           label = "Error Tipo II\n(β)", size = 3, color = "#377EB8") +
  scale_color_manual(values = c("#E41A1C", "#377EB8")) +
  scale_fill_manual(values = c("#E41A1C", "#377EB8")) +
  labs(x = "Estadístico de prueba", y = "Densidad",
       color = "", fill = "") +
  theme_minimal() +
  theme(legend.position = "bottom")
```

### Poder estadístico

El **poder** de una prueba es la capacidad de detectar un efecto real cuando existe (1 - probabilidad de Error Tipo II).

Un estudio con poder de 0.80 significa que, si el efecto existe, lo detectaremos el 80% de las veces.

¿Cómo aumentar el poder?

1. **Aumentar el tamaño de muestra** (más datos = más precisión)
2. **Efectos más grandes son más fáciles de detectar**
3. **Menor variabilidad en los datos ayuda**

## Prueba t para una media

### Ejemplo: Satisfacción con la democracia

Supongamos que el promedio latinoamericano de satisfacción con la democracia es 5.0 (en escala 1-10). Queremos saber si Chile difiere de este promedio.

```{r}
# Datos simulados de una encuesta en Chile
set.seed(2024)
satisfaccion <- rnorm(150, mean = 4.2, sd = 2.1)
satisfaccion <- pmax(1, pmin(10, satisfaccion))  # Limitar a escala 1-10
```

**Nuestras hipótesis:**

- H₀: La satisfacción promedio en Chile es igual a 5.0
- H₁: La satisfacción promedio en Chile es diferente de 5.0

**Realizamos la prueba en R:**

```{r}
resultado <- t.test(satisfaccion, mu = 5.0)
resultado
```

### Cómo leer el output de `t.test()`

```{r}
#| echo: false
cat("	One Sample t-test

data:  satisfaccion
t = -4.8123, df = 149, p-value = 3.542e-06
alternative hypothesis: true mean is not equal to 5
95 percent confidence interval:
 4.020159 4.534641
sample estimates:
mean of x
   4.2774")
```

Interpretación línea por línea:

| Elemento | Qué significa |
|----------|---------------|
| `t = -4.8123` | El estadístico de prueba. Valores más alejados de 0 indican mayor diferencia |
| `df = 149` | Grados de libertad (n - 1) |
| `p-value = 3.542e-06` | p = 0.0000035. Extremadamente pequeño |
| `95 percent confidence interval` | Con 95% de confianza, la media real está entre 4.02 y 4.53 |
| `mean of x = 4.2774` | La media observada en nuestra muestra |

**Interpretación en lenguaje simple:**

```{r}
#| echo: false
if(resultado$p.value < 0.05) {
  cat("Conclusión: El valor p es menor que 0.05, por lo tanto rechazamos H₀.\n\n")
  cat("La satisfacción promedio en Chile (", round(mean(satisfaccion), 2),
      ") es significativamente diferente del promedio latinoamericano (5.0).\n\n", sep = "")
  cat("El intervalo de confianza [", round(resultado$conf.int[1], 2),
      " - ", round(resultado$conf.int[2], 2),
      "] no incluye el valor 5.0, lo que confirma la diferencia.", sep = "")
}
```

::: {.callout-note .callout-avanzado collapse="true"}
## 🎓 Avanzado: Fórmula del estadístico t

El estadístico t mide cuántos "errores estándar" está nuestra media muestral del valor hipotético:

$$t = \frac{\bar{x} - \mu_0}{s / \sqrt{n}}$$

Donde:
- $\bar{x}$ = media muestral
- $\mu_0$ = valor hipotético (5.0 en nuestro ejemplo)
- $s$ = desviación estándar muestral
- $n$ = tamaño de muestra

```{r}
# Cálculo manual
media_muestral <- mean(satisfaccion)
desv_estandar <- sd(satisfaccion)
n <- length(satisfaccion)
mu_0 <- 5.0

t_calculado <- (media_muestral - mu_0) / (desv_estandar / sqrt(n))

cat("Media muestral:", round(media_muestral, 2), "\n")
cat("Desviación estándar:", round(desv_estandar, 2), "\n")
cat("Estadístico t:", round(t_calculado, 2), "\n")

# Valor p (bilateral)
valor_p <- 2 * pt(abs(t_calculado), df = n - 1, lower.tail = FALSE)
cat("Valor p:", round(valor_p, 6), "\n")
```
:::

## Prueba t para dos grupos independientes

Frecuentemente queremos comparar dos grupos. Por ejemplo: ¿hay diferencia en confianza institucional entre hombres y mujeres?

```{r}
# Datos simulados de confianza en el Congreso (escala 1-10)
set.seed(2024)
confianza_hombres <- rnorm(120, mean = 3.8, sd = 1.8)
confianza_mujeres <- rnorm(130, mean = 3.2, sd = 1.9)

confianza_hombres <- pmax(1, pmin(10, confianza_hombres))
confianza_mujeres <- pmax(1, pmin(10, confianza_mujeres))
```

**Hipótesis:**

- H₀: No hay diferencia entre hombres y mujeres
- H₁: Sí hay diferencia

```{r}
resultado_2grupos <- t.test(confianza_hombres, confianza_mujeres)
resultado_2grupos
```

### Interpretando la comparación de grupos

```{r}
#| echo: false
diferencia <- mean(confianza_hombres) - mean(confianza_mujeres)
cat("Diferencia de medias: Hombres (", round(mean(confianza_hombres), 2),
    ") - Mujeres (", round(mean(confianza_mujeres), 2),
    ") = ", round(diferencia, 2), " puntos\n\n", sep = "")

if(resultado_2grupos$p.value < 0.05) {
  cat("Con p =", round(resultado_2grupos$p.value, 4), "< 0.05, hay diferencia estadísticamente significativa.\n")
  cat("Los hombres reportan mayor confianza en el Congreso que las mujeres.")
} else {
  cat("Con p =", round(resultado_2grupos$p.value, 4), ">= 0.05, no hay diferencia estadísticamente significativa.")
}
```

```{r}
#| label: fig-boxplot-genero
#| fig-cap: "Confianza en el Congreso por género"
#| code-fold: true

datos_genero <- data.frame(
  confianza = c(confianza_hombres, confianza_mujeres),
  genero = factor(c(rep("Hombres", length(confianza_hombres)),
                    rep("Mujeres", length(confianza_mujeres))))
)

ggplot(datos_genero, aes(x = genero, y = confianza, fill = genero)) +
  geom_boxplot(alpha = 0.7) +
  geom_jitter(width = 0.2, alpha = 0.3, size = 1) +
  stat_summary(fun = mean, geom = "point", shape = 18, size = 4, color = "red") +
  scale_fill_manual(values = c("#4ECDC4", "#FF6B6B")) +
  labs(x = "", y = "Confianza en el Congreso (1-10)",
       caption = "El diamante rojo indica la media de cada grupo") +
  theme_minimal() +
  theme(legend.position = "none")
```

## Prueba chi-cuadrado para variables categóricas

Cuando ambas variables son categóricas, usamos chi-cuadrado para evaluar si están asociadas.

### Ejemplo: Educación y preferencia de voto

¿Existe relación entre el nivel educativo y la preferencia política?

```{r}
# Crear tabla de contingencia
voto_educacion <- matrix(c(
  45, 35, 20,   # Educación básica
  40, 45, 35,   # Educación media
  25, 40, 50    # Educación superior
), nrow = 3, byrow = TRUE)

rownames(voto_educacion) <- c("Básica", "Media", "Superior")
colnames(voto_educacion) <- c("Izquierda", "Centro", "Derecha")

voto_educacion
```

**Hipótesis:**

- H₀: Educación y voto son independientes (no están relacionados)
- H₁: Existe asociación entre educación y voto

```{r}
resultado_chi <- chisq.test(voto_educacion)
resultado_chi
```

### Cómo leer el output de `chisq.test()`

| Elemento | Qué significa |
|----------|---------------|
| `X-squared` | El estadístico chi-cuadrado. Valores mayores indican mayor asociación |
| `df` | Grados de libertad = (filas - 1) × (columnas - 1) |
| `p-value` | La probabilidad de ver esta asociación por azar |

**Interpretación:**

```{r}
#| echo: false
if(resultado_chi$p.value < 0.05) {
  cat("Con p =", round(resultado_chi$p.value, 4), "< 0.05, rechazamos H₀.\n")
  cat("Existe una asociación estadísticamente significativa entre nivel educativo y preferencia de voto.")
} else {
  cat("Con p =", round(resultado_chi$p.value, 4), ">= 0.05, no rechazamos H₀.\n")
  cat("No hay evidencia de asociación entre educación y voto.")
}
```

### Entendiendo los residuos

Los **residuos estandarizados** nos dicen qué combinaciones específicas son más o menos frecuentes de lo esperado:

```{r}
round(resultado_chi$residuals, 2)
```

- Valores **mayores a +2**: Más casos de los esperados
- Valores **menores a -2**: Menos casos de los esperados

::: {.callout-note}
## Interpretación de los residuos

En este ejemplo:
- Personas con educación **básica** votan más por la **izquierda** de lo esperado (residuo positivo alto)
- Personas con educación **superior** votan más por la **derecha** de lo esperado
- Personas con educación **básica** votan menos por la **derecha** de lo esperado (residuo negativo)
:::

```{r}
#| label: fig-mosaico
#| fig-cap: "Relación entre nivel educativo y preferencia de voto"
#| code-fold: true

library(tidyr)
datos_voto <- as.data.frame(as.table(voto_educacion))
names(datos_voto) <- c("Educacion", "Voto", "Frecuencia")

ggplot(datos_voto, aes(x = Educacion, y = Frecuencia, fill = Voto)) +
  geom_bar(stat = "identity", position = "fill") +
  scale_fill_manual(values = c("#E41A1C", "#984EA3", "#377EB8")) +
  scale_y_continuous(labels = scales::percent) +
  labs(x = "Nivel educativo", y = "Proporción", fill = "Preferencia") +
  theme_minimal()
```

## Significancia estadística vs. significancia práctica

Esta es posiblemente la distinción más importante de todo el capítulo.

::: {.callout-important}
## La distinción crucial

**Significancia estadística** (p < 0.05) solo significa que el efecto probablemente no es exactamente cero.

**Significancia práctica** pregunta: ¿el efecto es lo suficientemente grande como para importar en el mundo real?
:::

### Ejemplo: Un efecto "significativo" pero irrelevante

Un programa de educación cívica aumenta el conocimiento político de 50.0 a 50.5 puntos (en escala de 0-100). Con una muestra de 10,000 personas:

```{r}
set.seed(2024)
control <- rnorm(5000, mean = 50, sd = 15)
tratamiento <- rnorm(5000, mean = 50.5, sd = 15)

t.test(tratamiento, control)
```

El resultado es "estadísticamente significativo" (p < 0.05). ¡Pero la diferencia es solo medio punto en 100!

¿Vale la pena implementar un programa costoso para ganar 0.5 puntos?

### Qué reportar siempre

Para evaluar correctamente un resultado, necesitas:

1. **El tamaño del efecto:** ¿Cuál es la diferencia real? (no solo si es significativa)
2. **El intervalo de confianza:** ¿Cuál es el rango plausible del efecto?
3. **El contexto práctico:** ¿Esta diferencia importa en el mundo real?

```{r}
#| echo: false
cat("En nuestro ejemplo:\n")
cat("- Diferencia de medias: 0.5 puntos\n")
cat("- Esto representa solo el", round((0.5/100)*100, 1), "% de la escala\n")
cat("- Aunque es 'significativo', probablemente no justifica el costo del programa")
```

::: {.callout-note .callout-avanzado collapse="true"}
## 🎓 Avanzado: d de Cohen (tamaño del efecto)

Una medida estandarizada del tamaño del efecto es la **d de Cohen**:

$$d = \frac{\bar{x}_1 - \bar{x}_2}{s_{pooled}}$$

Interpretación convencional:
- d = 0.2: efecto pequeño
- d = 0.5: efecto mediano
- d = 0.8: efecto grande

```{r}
diferencia <- mean(tratamiento) - mean(control)
s_pooled <- sqrt((var(tratamiento) + var(control)) / 2)
d_cohen <- diferencia / s_pooled

cat("d de Cohen:", round(d_cohen, 3), "\n")
cat("Interpretación: efecto",
    ifelse(abs(d_cohen) < 0.2, "trivial",
           ifelse(abs(d_cohen) < 0.5, "pequeño",
                  ifelse(abs(d_cohen) < 0.8, "mediano", "grande"))))
```

### El problema de las comparaciones múltiples

Si pruebas muchas hipótesis, algunas darán "significativas" por puro azar:

```{r}
# 20 pruebas cuando H0 siempre es cierta
set.seed(2024)
valores_p <- replicate(20, {
  x <- rnorm(50)  # Datos puramente aleatorios
  y <- rnorm(50)
  t.test(x, y)$p.value
})

cat("De 20 pruebas con datos aleatorios:\n")
cat("Número de 'significativos' (p < 0.05):", sum(valores_p < 0.05), "\n")
cat("¡Esto ocurre incluso cuando NO hay ningún efecto real!")
```

**Corrección de Bonferroni:** Si haces m pruebas, usa α' = α/m como umbral.
:::

## Errores comunes que debes evitar

::: {.callout-caution}
## Lista de verificación: Errores a evitar

| Error común | Por qué es problema |
|------------|---------------------|
| "p = 0.03 significa 3% de que H₀ sea cierta" | El valor p no es la probabilidad de que H₀ sea verdadera |
| Tratar p = 0.049 y p = 0.051 como muy diferentes | Son prácticamente idénticos; el umbral 0.05 es arbitrario |
| "No significativo" = "no hay efecto" | Solo significa que no detectamos efecto, no que no exista |
| Probar muchas hipótesis y reportar solo las significativas | Esto es "p-hacking" y genera falsos positivos |
| Ignorar el tamaño del efecto | Un efecto significativo puede ser prácticamente irrelevante |
| No reportar intervalos de confianza | El valor p solo no cuenta toda la historia |
:::

## Resumen: Guía rápida de pruebas de hipótesis {.unnumbered}

### Conceptos clave

| Concepto | Significado |
|----------|-------------|
| H₀ (hipótesis nula) | "No hay efecto" - lo que asumimos inicialmente |
| H₁ (hipótesis alternativa) | Lo que buscamos demostrar |
| Valor p | Probabilidad de ver estos datos si H₀ fuera cierta |
| α (nivel de significancia) | Umbral de decisión (usualmente 0.05) |
| Error Tipo I | Rechazar H₀ cuando es verdadera (falso positivo) |
| Error Tipo II | No rechazar H₀ cuando es falsa (falso negativo) |

### Pruebas en R

| Situación | Función en R | Ejemplo |
|-----------|--------------|---------|
| Comparar una media con un valor | `t.test(x, mu = valor)` | ¿La satisfacción difiere de 5? |
| Comparar medias de dos grupos | `t.test(grupo1, grupo2)` | ¿Difieren hombres y mujeres? |
| Asociación entre categóricas | `chisq.test(tabla)` | ¿Educación y voto están relacionados? |
| Comparar una proporción | `prop.test(éxitos, n, p = proporción)` | ¿El apoyo difiere de 50%? |

### Mensaje final

**Recuerda siempre:** Significancia estadística ≠ importancia práctica. Un resultado puede ser estadísticamente significativo pero trivial en la práctica, o puede ser importante pero no alcanzar significancia por muestra pequeña.

Siempre reporta: valor p + tamaño del efecto + intervalo de confianza + interpretación sustantiva.

## Lecturas recomendadas {.unnumbered}

**Fundamentos de pruebas de hipótesis:**

Agresti, A., & Finlay, B. (2009). *Statistical Methods for the Social Sciences* (4th ed.). Pearson.
→ Capítulo 6 ofrece explicación rigurosa pero accesible de la lógica de pruebas de significancia estadística.

**Aplicaciones en ciencias sociales:**

Llaudet, E., & Imai, K. (2022). *Data Analysis for Social Science: A Friendly and Practical Introduction*. Princeton University Press.
→ Sección 7.5 conecta pruebas de hipótesis con aplicaciones prácticas en investigación social y política.

**Sobre problemas y mal uso de valores p:**

Wasserstein, R. L., & Lazar, N. A. (2016). The ASA's statement on p-values: Context, process, and purpose. *The American Statistician*, 70(2), 129-133.
→ Declaración oficial de la Asociación Americana de Estadística sobre interpretación correcta de valores p.

## Ejercicios {.unnumbered}

::: {.ejercicios}

**1. Formulación de hipótesis**

Para cada escenario, formula H₀ y H₁ apropiadas:

a) Un programa de becas pretende aumentar la asistencia escolar, actualmente en 85%
b) Una nueva ley electoral busca modificar la participación (sin expectativa de dirección)
c) Un investigador cree que la desigualdad ha aumentado respecto al nivel de 2015 (Gini = 0.48)

**2. Prueba t para una muestra**

Un índice de satisfacción democrática tiene media latinoamericana de 5.0 (escala 1-10). Con datos de Chile:

```{r eval=FALSE}
set.seed(123)
satisfaccion_chile <- rnorm(120, mean = 4.3, sd = 2.1)
satisfaccion_chile <- pmax(1, pmin(10, satisfaccion_chile))
```

a) Formula las hipótesis
b) Realiza prueba t con `t.test()`
c) Interpreta el valor p en palabras simples
d) ¿Qué nos dice el intervalo de confianza?
e) Escribe una conclusión en lenguaje no técnico

**3. Prueba chi-cuadrado**

Analiza esta tabla de preferencia política según nivel educativo:

```{r eval=FALSE}
tabla <- matrix(c(
  45, 30, 25,
  35, 40, 45,
  20, 50, 60
), nrow = 3, byrow = TRUE)
rownames(tabla) <- c("Básica", "Media", "Superior")
colnames(tabla) <- c("Izquierda", "Centro", "Derecha")
```

a) Realiza prueba chi-cuadrado
b) Examina los residuos: ¿qué celdas destacan?
c) Interpreta los resultados en términos sustantivos

**4. Significancia estadística vs. práctica**

Un programa aumenta el conocimiento político de 50 a 50.8 puntos (escala 0-100). Con n=5000, p < 0.001.

a) ¿El resultado es estadísticamente significativo?
b) ¿El resultado es sustantivamente importante? Argumenta
c) ¿Qué información adicional necesitarías para decidir si el programa vale la pena?

:::

